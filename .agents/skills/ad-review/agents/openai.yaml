interface:
  display_name: ad-review
  short_description: Fresh-context code review per WORKFLOW §10 — assemble handoff, /clear, return adversarial findings.
policy:
  allow_implicit_invocation: false
